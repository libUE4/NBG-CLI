---
title: "API Key (Simple Setup)"
sidebarTitle: "API Key"
description: "在 NBG 中通过 Bedrock API Key 快速配置 AWS Bedrock。"
---

### 概览

-   **AWS Bedrock：** AWS 托管的生成式 AI 服务，可访问 Anthropic Claude、Amazon Nova 等模型。\
    参考 [AWS Bedrock 文档](https://docs.aws.amazon.com/bedrock/latest/userguide/what-is-bedrock.html)。
-   **NBG：** 通过 provider 配置接入 Bedrock 模型，用于代码生成、调试、分析和自动化任务。
-   **适用场景：** 适合个人开发者或快速验证场景。企业生产环境仍建议优先使用 SSO/federated roles 或短期凭证。

---

### Step 1: 准备 AWS 环境

#### 1.1 创建 Bedrock API key

详细步骤请参考 [AWS Bedrock API key 文档](https://docs.aws.amazon.com/bedrock/latest/userguide/api-keys.html)。

1. **登录 AWS Management Console：**\
   [AWS Console](https://aws.amazon.com/console/)
2. **打开 Bedrock Console：**
    - [Bedrock Console](https://console.aws.amazon.com/bedrock)
    - 创建新的 Long Lived API Key。该 API key 默认关联 `AmazonBedrockLimitedAccess` IAM policy。
      [查看 AmazonBedrockLimitedAccess policy 详情](https://docs.aws.amazon.com/bedrock/latest/userguide/security-iam.html)

#### 1.2 创建或调整 policy

为了让 NBG 调用 AWS Bedrock，IAM user 或 role 需要相应权限。`AmazonBedrockLimitedAccess` managed policy 覆盖范围较广；如果希望遵循最小权限原则，核心模型调用通常需要：

-   `bedrock:InvokeModel`
-   `bedrock:InvokeModelWithResponseStream`
-   `bedrock:CallWithBearerToken`

你可以创建自定义 IAM policy，并将其绑定到对应 IAM user 或 role。

1.  在 AWS IAM console 中创建新 policy。
2.  使用 JSON editor 添加以下 policy document：
    ```json
    {
        "Version": "2012-10-17",
        "Statement": [
            {
                "Effect": "Allow",
                "Action": ["bedrock:InvokeModel", "bedrock:InvokeModelWithResponseStream", "bedrock:CallWithBearerToken"],
                "Resource": "*"
            }
        ]
    }
    ```
3.  为 policy 命名，例如 `NBGBedrockInvokeAccess`，并绑定到你创建 API key 时关联的 IAM user。该 IAM user 与 API key 通常具有相同前缀。

**重要说明：**

-   **模型列表：** 如果在 NBG 中直接填写 model ID，最小权限通常足够调用模型。若需要动态列出可用 Bedrock 模型，可能还需要 `bedrock:ListFoundationModels`。
-   **AWS Marketplace 订阅：** 第三方模型，例如 Anthropic Claude，可能需要在 AWS Marketplace 或 Bedrock Console 中完成订阅和 First Time Use (FTU) 表单。如果收到 `Model use case details have not been submitted for this account`，请打开 [AWS Bedrock Console Playground](https://console.aws.amazon.com/bedrock/home?#/text-generation-playground)，选择对应 Anthropic 模型并填写用途信息。

---

### Step 2: 确认区域访问

#### 2.1 选择并确认 Region

1. **选择 Region：**\
   AWS Bedrock 在多个区域可用，例如 US East、Europe、Asia Pacific。根据延迟、合规和团队策略选择区域。\
   [AWS Global Infrastructure](https://aws.amazon.com/about-aws/global-infrastructure/regions_az/)
2. **验证模型访问：**
    - 部分模型只能通过 [Inference Profile](https://docs.aws.amazon.com/bedrock/latest/userguide/inference-profiles-support.html) 访问。此类模型需要在 NBG 中勾选 "Cross Region Inference"。

---

### Step 3: 在 NBG 中配置

#### 3.1 安装并打开 NBG

如果尚未安装 NBG，请参考 [Getting Started](/getting-started/installing-cline)。

#### 3.2 配置 NBG 设置

1. **打开 NBG 设置：**
    - 点击设置图标 ⚙️ 进入 API Provider 配置。
2. **选择 AWS Bedrock provider：**
    - 在 "API Provider" 下拉菜单中选择 **AWS Bedrock**。
3. **填写 AWS API key：**
    - 输入 **API Key**。
    - 选择正确的 **AWS Region**，例如 `us-east-1` 或企业批准的区域。
4. **选择模型：**
    - 选择可用的 Bedrock provider model。
5. **保存并测试：**
    - 点击 **Done/Save** 应用设置。
    - 发送一个简单请求验证连接，例如生成一个判断质数的 Python 函数。

---

### Step 4: 安全、监控与最佳实践

1. **访问安全：**
    - 可行时优先使用 AWS SSO/federated roles，而不是长期 API key。
    - [AWS IAM Best Practices](https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html)
2. **网络安全：**
    - 可考虑使用 [AWS PrivateLink](https://docs.aws.amazon.com/vpc/latest/userguide/endpoint-services-overview.html) 安全连接 Bedrock。
3. **监控与日志：**
    - 启用 AWS CloudTrail 记录 Bedrock API 调用。
    - 使用 CloudWatch 监控调用次数、延迟和 token 用量。
    - 为异常活动设置告警。
4. **错误处理与成本管理：**
    - 对 throttling errors 实现指数退避。
    - 使用 AWS Cost Explorer 和账单告警跟踪使用量。\
      [AWS Cost Management](https://docs.aws.amazon.com/cost-management/latest/userguide/what-is-aws-cost-management.html)
5. **审计与合规：**
    - 定期审查 IAM roles 和 CloudTrail logs。
    - 遵守内部数据隐私和治理策略。

---
