---
title: "认证配置"
description: "为 NBG 配置 provider 并选择第一个 AI 模型"
---

NBG 通过 **provider** 连接 AI 模型。当前保留 Cline 兼容 provider，同时默认支持 OpenAI-compatible、Anthropic、OpenAI、Google Gemini、OpenRouter 等 BYOK 路径。

- **OpenAI-compatible / BYOK**：使用自己的 provider 凭据，适合商业化部署和自有模型网关。
- **Cline 兼容 provider**：使用继承运行时中的 OAuth provider id `cline`。

## 目录

- [CLI 配置](#cli-配置)
- [IDE 配置](#ide-配置)

## CLI 配置

```bash
# 打开交互式认证设置
nbg auth

# 使用 Cline 兼容 provider OAuth
nbg auth cline

# 使用 OpenAI-compatible 或其他 BYOK provider
nbg auth --provider openai-compatible --apikey <key> --baseurl <url> --modelid <model>
```

无交互运行时，如果选择了 OAuth provider 但没有已保存凭据，`nbg` 会直接给出认证错误，不会隐藏式打开浏览器。

## IDE 配置

<Steps>
  <Step title="打开 NBG 设置">
    点击 NBG 面板中的设置图标。
  </Step>

  <Step title="选择 provider">
    从 **API Provider** 下拉框选择目标 provider。
  </Step>

  <Step title="配置认证">
    - **BYOK provider:** 在 **API Key** 字段粘贴你的 key。
    - **Cline 兼容 provider:** 点击 **Sign In** 并完成 OAuth。
    - **本地运行时（Ollama/LM Studio）:** 不需要 key，确认本地服务已运行。
  </Step>

  <Step title="选择模型">
    从 **Model** 下拉框选择模型。
  </Step>
</Steps>

## Provider 选项

### BYOK 和 OpenAI-compatible

适合把 NBG 接入自有模型网关、云厂商或本地推理服务。生产环境优先把 key 放在本地配置或环境变量中，不要提交到仓库。

### Cline 兼容 provider

- 继承运行时中的 provider id 是 `cline`
- 支持 OAuth 登录
- 用于兼容已有账号、配置和测试路径

NBG 不把旧 Dashboard 作为默认商业化入口。需要使用该兼容 provider 时，通过 `nbg auth cline` 或 IDE 登录入口完成认证。

### BYOK（云端和本地）

### 云端 provider

| Provider | 适用场景 | 配置指南 |
|----------|----------|-------------|
| **OpenRouter** | 多模型接入和灵活价格 | [配置](/provider-config/openrouter) |
| **Anthropic** | 直连 Claude API | [配置](/provider-config/anthropic) |
| **Claude Code** | Claude Max/Pro 订阅 | [配置](/provider-config/anthropic) |
| **OpenAI** | GPT 模型 | [配置](/provider-config/openai) |
| **Google Gemini** | Gemini 模型 | [配置](/provider-config/google-gemini) |
| **AWS Bedrock** | 企业云环境 | [配置](/provider-config/aws-bedrock/api-key) |
| **DeepSeek** | 高性价比模型 | [配置](/provider-config/deepseek) |

### 本地模型

在自己的硬件上运行模型，适合需要本地隐私控制或避免按请求计费的场景。

| Provider | 适用场景 | 配置指南 |
|----------|----------|-------------|
| **Ollama** | 命令行本地运行时 | [配置](/running-models-locally/overview#runtime-options) |
| **LM Studio** | 图形界面本地运行时 | [配置](/running-models-locally/overview#runtime-options) |

本地模型需要足够硬件资源，尤其是 GPU 显存。硬件要求见 [本地运行模型](/running-models-locally/overview)。

## 故障排查

| 问题 | 处理方式 |
|-------|-----|
| "Unauthorized: Please sign in" | Session 已过期。点击 **Sign In** 重新认证。 |
| 浏览器没有打开 | 检查默认浏览器设置，或从 NBG 输出面板手动复制 URL。 |
| 频繁要求重新认证 | 检查组织安全策略，确认 IDE secrets 没有被清理，再完整退出并重新登录。 |
| 无法访问组织 | 用兼容 provider 账号确认组织成员资格，或联系管理员检查权限，然后重新登录。 |
